plugins {
    id 'java'
}

group 'de.plixo'
version '1.0'

repositories {
    mavenCentral()
}

dependencies {
}

jar {
    manifest {
        attributes "Main-Class": "de.plixo.subzip.Main"
        attributes "Class-Path": configurations.compile.collect { it.absolutePath }.join(" ")
    }
}